services:
  app:
    build: ./app
    ports:
      - "127.0.0.1:8000:8000"
    depends_on:
      - db

  db:
    image: postgres:16
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
